# createSingletonPromise

## 描述

创建一个单例 Promise。

## 示例

````ts
import { createSingletonPromise } from '@base-web-kits/base-tools-vue';

const createPromise = createSingletonPromise(async () => {
  return 'hello';
});
``` 示例代码
````

## 来源

[VueUse](https://vueuse.org/functions/createSingletonPromise/)
