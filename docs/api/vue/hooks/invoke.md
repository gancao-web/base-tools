# invoke

## 描述

立即执行一个函数。

## 示例

````ts
import { invoke } from '@base-web-kits/base-tools-vue';

const result = invoke(() => {
  return 1 + 1;
});
```来源

[VueUse](https://vueuse.org/functions/invoke/)
````
