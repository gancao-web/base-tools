# uniq

创建一个去重后的数组副本。

## 示例

```ts
import { uniq } from '@base-web-kits/base-tools-ts';

uniq([2, 1, 2]);
// 结果: [2, 1]
```

## 参数

- `array (Array)`: 要检查的数组。

## 返回值

- `(Array)`: 返回去重后的新数组。

## 来源

- [es-toolkit uniq](https://es-toolkit.dev/zh_hans/reference/array/uniq.html)
