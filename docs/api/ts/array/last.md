# last

获取数组的最后一个元素。

## 示例

```ts
import { last } from '@base-web-kits/base-tools-ts';

last([1, 2, 3]);
// 结果: 3
```

## 参数

- `array (Array)`: 要查询的数组。

## 返回值

- `(any)`: 返回数组的最后一个元素。

## 来源

- [es-toolkit last](https://es-toolkit.dev/zh_hans/reference/array/last.html)
