# Maintaining Notes

## npx skills

https://www.skills.sh/gancao-web/base-tools/base-tools

要实现 `npx skills add` 的安装行为，需满足以下要求：

1. 在 `GitHub` 主仓库根目录创建 `skills` 目录
2. 把 `SKILL.md` 分类存放到 `skills` 目录下
3. 检查 `SKILL.md` 的 `name` 和 `description`，必须使用单引号包裹
